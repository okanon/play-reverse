# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET    /$path<.*> controllers.RootController.reverseProxy(path)
POST   /$path<.*> controllers.RootController.reverseProxy(path)
HEAD   /$path<.*> controllers.RootController.reverseProxy(path)
DELETE /$path<.*> controllers.RootController.reverseProxy(path)
